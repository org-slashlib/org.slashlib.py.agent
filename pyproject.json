{
  "name": "Python Agent",
  "version": "0.1.2",
  "paths": {
    "root":         "{ROOT}",
    "assets":       "{ROOT}/assets",
    "downloads":    "{ROOT}/downloads",
    "tmp":          "{ROOT}/tmp"
  },
  "assets": {
    "keys":         "{paths.assets}/ainode.keys.json",
    "logging":      "{paths.assets}/logging.json"
  },
  "adapter": {
    "ollama": {
      "model":      "gemma4",
      "think":      true,
      "timeout":    600.0
    }
  }
}
